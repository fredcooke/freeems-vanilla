FreeEMS release procedure

update the interface and version fields length in header too if changed
update serial documentation
update interface data map
make clean all; # to check that it builds cleanly - check win/osx too if possible
update changelog and bugs and tasks - check mantis and git log since last release
generate doxygen - setup something of a structure on docs.freeems.org for this
update all documentation
generate pdfs from open document files
git commit;git push; git push alternate
cd src;make release
upload all zips to sourceforge or somewhere else?(sftp fredcooke@frs.sourceforge.net, password, cd uploads, put *.bz2)
check "keep my format" buttons in sourceforge
update goals,firmware,hardware threads as appropriate linking sourceforge,video,pics
increment version number in makefile and globals for next iteration + commit and push
